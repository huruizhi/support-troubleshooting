interface:
  display_name: "Support Ticket Triage"
  short_description: "Choose quick, MCP, or local-log ticket triage"
  default_prompt: "Use $support-ticket-triage to analyze this ticket through the lightest sufficient input path."

dependencies:
  tools:
    - type: "mcp"
      value: "ticket-investigation"
      description: "Read-only technical support ticket and artifact access"
      transport: "streamable_http"
      url: "https://support.gitlab.cn/mcp/ticket-investigation"
